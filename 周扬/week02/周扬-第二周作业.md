#本周作业是给出海量5维向量，推理出哪个向量最大，就把这个向量值所在的索引打印出来
#实际是一个五分类的输出维度，即最后的输出层是5个值，最大的那个就是结果所在的索引

CnnWeek2.py为模型主程序，包含训练以及测试
TestModel.py为测试模型的程序，主要用于训练过程中给出每轮测试的准确率
CreateSample.py为创建样本的程序，主要用于创建测试数据
CnnWeek2.m为模型的参数结果文件，存储训练的参数

